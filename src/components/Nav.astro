---
const currentSite = Astro.canonicalURL.pathname
---

<style>
  main {
    background-color: #333;
    border-radius: 15px;
    margin: 8px;
  } 
  main a{
    float: right;
    color: #f2f2f2;
    text-align: center;
    padding: 14px 16px;
    text-decoration: none;
    font-size: 115%;
    font-family:monospace;
  }
  main > a.logo {
    float: left;
    padding: 0;
    padding-left:15px;
    padding-top:7px;
  }
  .logo{
    width: 7%;
  }
  main a:hover:not(.logo,.active) {
    color: #ccc;
  }
    main a:hover:not(.logo)::before {
    position: fixed;
    content: ">";
  }
  main a.active {
    color:#FF6625;
  }

    @media(max-width:800px) {
    main a{
      display:block;
    }
    .logo{
      width: 25%;
      position: absolute;
      float: none;
     }
    main a{
      float: none;
      color: #f2f2f2;
      padding:0px 0px;
      padding-top:7px;
      font-size: 14px;
      margin: 2px;
     }
  }
</style>
<header>
  <a href="/" class="logo"><img src="/vlogo-r.svg" ></a>
  <a href="/blog" class={(currentSite.startsWith('/blog/')) ? "active" : ""}>&nbsp Blog</a>
  <a href="/contact" class={(currentSite == '/contact/') ? "active" : ""}>&nbsp Contact</a>
  <a href="/aboutme" class={(currentSite == '/aboutme/') ? "active" : ""}>&nbsp About me</a>
  <a href="/" class={(currentSite == '/') ? "active" : ""}>&nbsp Home</a>
</header> 